import { TalkDirective } from './talk.directive';

describe('TalkDirective', () => {
  it('should create an instance', () => {
    const directive = new TalkDirective();
    expect(directive).toBeTruthy();
  });
});
